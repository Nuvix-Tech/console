"use client";
import React from "react";

type Props = {
  databaseId: string;
  collectionId: string;
};

export const IndexesPage: React.FC<Props> = ({ databaseId, collectionId }) => {
  return <>IND</>;
};
